name: Build EntryClient Artifact

on:
  push:
    branches:
      - master

env:
  ENTRYCLIENT_VERSION: "4.6"

jobs:
  setup:
    name: Checkout Source
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

  build:
    name: Building the artifact
    runs-on: self-hosted
    container:
      image: epidata/fpcbuild:3.2.0-12
      volumes:
        - /etc/maven/settings.xml:/etc/maven/settings.xml
    needs: setup

    steps:
      - name: Compile Linux
        run:  mvn compile -Plinux64

      - name: Compile Windows
        run:  mvn compile -Pwin64

      - name: Compile Mac
        run:  mvn compile -Pdarwin64

  deploy:
    name: Deploy Artifact
    runs-on: self-hosted
    needs: build

    steps:
      - name: Deploy
        run: |
          mvn versions:set -DnewVersion=${ENTRYCLIENT_VERSION}.${GITHUB_RUN_NUMBER} -f deploy.pom.xml
          mvn deploy -f deploy.pom.xml
          mvn clean
